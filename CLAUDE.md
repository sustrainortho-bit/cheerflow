# CheerFlow プロジェクト

## 合言葉: 「CheerFlow引継」

ユーザーが **「CheerFlow引継」** と言ったら、以下を順番に実行すること：

1. `git pull` を実行して最新コードを取得する
2. `git log --oneline -10` で直近の変更履歴を表示する
3. 下記「現在の状態」を伝える
4. 「どこから続けますか？」と聞く

---

## プロジェクト概要

**CheerFlow** — チアリーディングのフォーメーション（隊形）・構成を作るためのエディタアプリ

| 項目 | 内容 |
|---|---|
| 位置づけ | 別のClaude.aiチャットでArtifactとして開発が進んでいたものを、2026-08-31にこちらへ引き継いだ |
| 技術構成 | 単一HTMLファイル（内蔵CSS/JS、フレームワーク不使用のvanilla JS） |
| 元のArtifact URL | https://claude.ai/code/artifact/09d327bc-f95a-4bc2-89c7-5ee5f435686e （オーナー所有） |

## ディレクトリ構成

```
cheerflow.html   # アプリ本体（これ1ファイルで完結。ビルド工程なし）
```

外部依存はGoogle Fonts（Oswald / Manrope / IBM Plex Mono）のみ。画像・フォントの埋め込みはなし。

## 次回セッションで最優先の修正タスク

CheerFlow本体への修正依頼は2026-09-03時点ですべて実装・実機確認済み、未着手なし。**次回はオーナーがiPadOS Scribble検証アプリ（下記）を実機で試した感触を聞くところから始めること。** 良好であればノート機能のグリッドUIをCheerFlow本体に実装するフェーズへ進む。

## 完了：ノート機能（手書き入力）のための事前検証（2026-09-03、2段階）

### 第1段階：汎用OCR（Tesseract.js）→ 実機検証の結果、不採用が確定

**オーナーの実機検証結果**：「文字の認識機能は実用に耐えるものではない」との判断。事前の技術検討で立てていた予想（汎用OCRは印刷文字向けの技術で日本語手書きには弱い）通りの結果になった。**Tesseract.js方式はこれで不採用が確定**。次点の**iPadOS Scribble方式**（Apple Pencil必須）で進めることが決定した。

検証アプリ（`https://github.com/sustrainortho-bit/handwriting-ocr-test` の`index.html`）はこの用途を終えたが、記録として残してある。

### 第2段階：iPadOS Scribble方式の試作（着手・オーナーの実機確認待ち）

Scribbleは「`<textarea>`/`<input>`にApple Pencilで直接書くとiPadOS側が自動でテキスト変換する」というOS標準機能のため、追加のOCRライブラリは一切不要（プレーンなHTMLフォーム要素を用意するだけ）。オーナーから「大きめのメモ欄」と「格子状のメモ欄」それぞれで、書いた文字が別の場所にテキストとして転記される様子を確認したいとの依頼があり、同じリポジトリに試作ページを追加した。

| 項目 | 内容 |
|---|---|
| 公開URL | **https://sustrainortho-bit.github.io/handwriting-ocr-test/scribble-test.html** |
| ファイル | 同リポジトリの`scribble-test.html`（`index.html`のTesseract版とは独立、相互にリンクあり） |

**画面構成**：
1. **大きめのメモ欄**：シーン全体メモを想定した1つの大きな`<textarea>`
2. **格子状のメモ欄**：シーン×カウント1〜8を想定した3行×8列の`<input>`グリッド（1セル76×52px、実際のノート機能に近いサイズ感）
3. **転記結果**：①②に入力された内容が、`input`イベントを介してリアルタイムでミラー表示される別領域（実際の機能では、ここが「フォーメーションボードのカウントメモ」に相当する）

**動作確認（Browserツールでの自動テスト）**：`input`イベントを発火させて値を書き込み、①の内容がそのまま、②の各セルが「カウント番号:内容」の形で③に正しくミラーされることを確認。クリアボタンで全欄がリセットされることも確認。ただしこれはキーボード入力のシミュレーションであり、**Apple PencilによるScribble変換そのものの実機確認はまだ**（iPadOS Safari + Apple Pencilでの実地検証が必要）。

**次回オーナーに確認してほしいこと**：iPadでApple Pencilを使い、①の大きい欄と②の小さいグリッドセルの両方で実際にScribbleが機能するか、特に②のような小さいマス目でも書きやすいか（マスが小さすぎるようならセルサイズの調整を検討）を確認する。問題なければ、この転記の仕組みをそのままCheerFlow本体のノート機能（`sc.countNotes`/`sc.note`との双方向反映）に組み込む設計に進める。

## 完了：2026-09-03 追加フィードバック6件

2026-09-02の9項目対応後、実機確認を経てさらに6件のフィードバックがあり対応した（うち1件は2つの不具合を含んでいたため下記は7項目）。すべて実装・実機確認済み（YouTube音源での実再生、実際に書き出したPNG/コンタクトシートのピクセル寸法・ピクセル値の検証を含む）。

1. **プレビューでコマの移動と導線の描画がずれる不具合を修正**：`runPresentTransitionAnimation()`は元々、コマの移動（CSS `transform`トランジション・`ease`カーブ）と導線の描画（`stroke-dashoffset`トランジション・`linear`カーブ）を別々のCSSトランジションとして走らせていたため、イージングカーブの違いにより「コマが先に大きく動いて、導線の描画だけ遅れて追いつく」ように見えていた。単一の`requestAnimationFrame`ループに書き換え、両者を同じ進捗値`e`から算出するように変更（コマの座標は導線要素の`getPointAtLength(e*len)`から直接取得、導線の`strokeDashoffset`も同じ`e`から算出）。実機でコマの座標と導線の描画進捗をフレームごとにサンプリングし、常に連動していることを確認。アニメーション終了後は導線の`stroke-dasharray`を元の値（例:「4, 4」）に復元するよう修正（副産物として見つけた不具合：終了後ずっと`stroke-dasharray`が巨大な値のまま残り、点線ではなく実線に見えてしまっていた）
2. **8カウント自動生成を「1カウント目のみ」から「1カウント目＋終点」の範囲指定方式に変更**：これまでBPMと「1カウント目」の1点だけを指定すると曲の最後までシーンを生成していたが、「終点」のマークを追加し、指定した区間内だけで8カウント間隔のシーンを生成するように変更（`ui.countSplit`に`markEnd`を追加、タイムライン上に②マークとオーバーレイ表示、生成範囲を`[markA, markEnd]`に限定）
3. **8カウント生成時のシーン番号ズレを解消**：従来は既存の「シーン1」（曲頭の未タイミング設定シーン）はそのまま残し、新規生成シーンを「シーン2」以降として追加していたため、実際の演技開始位置（1カウント目）と表示上のシーン番号が1つずれていた。`state.scenes[0]`の`startSec`を1カウント目の位置に直接設定し、そこを起点としてブロック2以降を追加する方式に変更。実機で全生成シーンの開始秒を確認し、1カウント目の位置から正確に8カウント間隔（BPM120なら4.00秒間隔）で並んでいることを確認
4. **8カウント自動生成時のシーン名自動付与を廃止**：生成される各シーンの`name`を空文字にし（「シーンN」を付けない）、表示側は既存の「無題」フォールバックに任せるように変更
5. **フォーメーションボード左上にシーン名（番号＋名前）を表示**：`renderStagePanel()`に`.stage-scene-title`を追加（右上の観客/演者視点ボタンとは反対側に配置し重ならないようにした）
6. **画像書き出しの解像度を3倍に引き上げ**：`EXPORT_SCALE=3`を新設し、「現在のシーンをPNG」「コンタクトシート」のラスタライズ解像度を1000×700→3000×2100に変更。実機で書き出したPNGのバイナリを`createImageBitmap`でデコードし、実際に3000×2100pxで出力されていることを確認
7. **コンタクトシートのセル間に余白と枠線を追加**：セルの隙間なく画像を敷き詰めていたため見分けにくかった問題を解消。セルサイズも320px→640pxに拡大。実機で書き出し画像のセル境界のピクセル値が背景色（白）になっている（＝実際に余白が空いている）ことをピクセル単位で確認

## 完了：2026-09-02 追加フィードバック9項目

14項目対応後、オーナーから実機確認を経て9件の追加フィードバックがあり対応した。すべて実装・実機確認済み（一部はFirestoreへの実際の保存・共有URL経由の読み込みで検証）。

1. **ホーム画面「プロジェクトを開く」で選択してもホーム画面に戻ってしまう不具合（根本原因判明・修正）**：`?p=<id>`の共有URLを読み込むページ起動時の処理で、Firestoreからの読み込み成功後に`applyLoadedState()`は呼んでいたが`ui.screen`を`'editor'`に切り替えていなかった。`ui.screen`の初期値は`'home'`のままなので、`render()`内の`if(ui.screen==='home' || !state.hasProject)`判定によりロード済みプロジェクトのデータがあってもホーム画面が表示され続けていた（一覧から選択→再読み込み→再びホーム画面、というループになっていた）。読み込み成功時に`ui.screen='editor'`を設定して解消
2. **「プロジェクトを開く」クリック時に作業中プロジェクトの続行確認**：`state.hasProject`が真の場合、クリック時に`confirm('作業中のプロジェクトがあります。続きから開始しますか？')`を表示。「はい」で即編集画面へ、「いいえ」でプロジェクト一覧へ
3. **音源に「始めから」ボタンを追加**：`renderTransportRow()`に追加し、`playbackSeek(0)`を実行
4. **コンタクトシートの複数シーン選択対応**：「コンタクトシート（選択中の1シーン）」を「（選択したシーン）」に変更。クリックするとチェックボックス付きのシーン一覧モーダル（現在のシーンがデフォルトで選択済み）が開き、選んだシーンだけを1枚の画像に並べて出力する`exportContactSheetPicked()`を新設
5. **プレビュー画面へカウント別メモを表示**：これまでボード下に横並びで小さく表示していたものを、ボード右の専用サイドパネル（`.present-countnotes-side`、幅200px）に移動。680px未満では縦積みレイアウトに切り替え
6. **プレビュー時のコマの滑らかな移動アニメーション**：`runPresentTransitionAnimation()`が実は元々（2026-08-31の引き継ぎ時点から）実装済みだったことが判明。実機で`getBoundingClientRect()`をフレームごとにサンプリングして検証したところ、実際にCSSトランジションで正しく補間移動していることを確認した。コード変更は不要
7. **フォーメーションボード上に客席の向き切り替えボタンを追加**：ボード右上に「⇅ 観客視点」/「⇅ 演者視点」ボタンを新設（`state.stage.audienceAtTop`）。押すと「客席」ラベルの表示位置が下/上に切り替わり、ボタンのラベルも連動する。**メンバーの配置データ自体は一切変更しない、表示上のトグルのみ**（実座標のミラー変換は行っていない）
8. **音源パネルのリサイズ時、動画サイズの反映が遅れる不具合を修正**：`#audioPanelResizer`のドラッグ処理（`apply(px)`）はCSS変数とlocalStorageの更新のみ行っており、YouTube動画を重ねて表示する`positionYtPortal()`（および`fitStageFrame()`）を呼んでいなかったため、次に何らかの理由で`render()`が走るまで動画サイズが古いままになっていた。`apply(px)`内で両関数を都度呼ぶように修正し、ドラッグ中リアルタイムに追従することを確認
9. **新規プロジェクトウィザードで構成名・人数の入力後にYouTube URL欄へ切り替えると入力が消える不具合を修正**：`wizardTitle`/`wizardCount`/`wizardYtUrl`の各`<input>`が非制御（値をuiステートに保持せず、送信時にDOMから直接読み取るだけ）だったため、音源タイプの切り替えボタンを押すたびに走る`render()`でinnerHTMLごと再生成され、入力済みの値が失われていた。`ui.wizardTitle`/`ui.wizardCount`/`ui.wizardYtUrl`を新設し、`input`イベントで同期・`value`属性に反映することで解消

## 完了：2026-09-02 14項目のうち9〜14

前回引き継ぎ時点でリストしていた14項目のうち、1〜8は同日の別セッションで先に対応済み（役割モデル再設計・スタンツ自動配置廃止・フッタースクロール修正・前後シーンボタン・フォーメーションコピペ・凡例重複削除など）。今回9〜14を実装し、実機（Browserツールでのエミュレーション、YouTube音源での実再生を含む）で全項目確認済み。

9. **再生位置のフッターハイライト**：`updateTimelineUI(t, dur)`から`updatePlayingHighlight(t)`を呼び、`sc.startSec<=t`な最後のシーンの`.thumb-card`に`playing`クラス（枠線＋▶バッジ）を付与。既存の直接DOM更新方式（`render()`を経由しない）を踏襲
10. **プレビューモードでの音源連動再生**：`hasAudioSource()`を新設。プレビュー開始時に対象シーンの`startSec`へ自動シークし自動再生、シーン送り時も0.5秒以上ずれていればシーク、`togglePresentPlay()`は音源がある場合は実際の`playbackTogglePlay()`に委譲（音源がない場合のみ従来の固定3.5秒タイマー）、プレビュー終了時は再生中なら一時停止
11. **コンタクトシート（選択中の1シーン）**：既存の全シーン版`exportContactSheetPng()`の中身を`exportContactSheet(scenes, filename)`に共通化し、`exportContactSheetCurrentPng()`を追加。エクスポートメニューに新規ボタン
12. **全シーン一覧PDFのレイアウト変更**：8シーンごとにページ分割し、1ページ＝2列×4行の`.print-cell`（フォーメーションボード＋シーンメモ＋8カウントメモをセットで表示）。`@page{ size:A4 portrait; }`と`display:grid`で構成
13. **ホーム画面サブタイトル削除**：`.home-tagline`の要素とCSSを削除
14. **ホーム画面「プロジェクトを開く」のプロジェクト一覧化**：オーナーとの相談の結果、「実データはFirestoreの`?p=<id>`で既に共有済み・URLさえ知っていればどの端末でも開ける」「不足していたのは端末ごとの一覧表示だけ」という整理になったため、**保存場所は変更せず**、`localStorage`に一覧インデックス（`cheerflow_project_list_v1`＝`{id, title, updatedAt}[]`）を追加する方式を採用。クラウド保存・クラウド読込のたびに自動でこの端末の一覧に追記/更新される。ホーム画面の「プロジェクトを開く」は、一覧が空でクラウド未使用の場合は従来通り直接エディタへ（一覧UIを挟まない・既存ユーザー体験を壊さない）、一覧が1件以上ある場合は新設の一覧画面（`ui.screen='projectList'`）を表示し、選択したプロジェクトの共有URL（`?p=<id>`）へ遷移する。一覧からの「×」削除はこの端末の表示から消すだけでFirestore上のデータは消さない
    - **既知の制約**：ある端末の一覧に、他人と共有しただけで自分がまだ開いていない（＝クラウド保存/読込をこの端末で一度もしていない）プロジェクトは出てこない。あくまで「この端末で開いた/保存したことがある」履歴であり、真の意味でのアカウント横断の一覧ではない

**副産物のバグ修正（今回の作業中に発見）**：2026-09-01のロール再設計（PART_DEFSをフライヤー/ベース/バックスポット/ダンサー/タンブラー/その他→トップ/ベース/スポッター/ハンズ/前サポーターに変更）の際、新規メンバー作成時のデフォルト値`part:'other'`を直し忘れており、新規プロジェクト作成・メンバー追加・一括追加のすべてで「存在しないpart値」を持つメンバーが生成され続けていた（実害：ロールバッジが永久に表示されない・名簿の選択欄が実際の値とズレて見える）。3箇所のデフォルト値を`PART_ORDER[0]`に修正し、あわせて`ensureMemberColors()`に`part`の妥当性チェック・自動修復も追加（過去に保存された`other`等の無効値を持つプロジェクトを開いた際にも自動移行される）。

**実機検証メモ（2026-09-02）**：本番URL（GitHub Pages）でYouTube音源の新規プロジェクトを作成し、以下を実際の再生・操作で確認した。
- 役割変更（トップ→ベース）が即座にボード上のバッジに反映される（7の再確認）
- コマ2つを選択してスタンツ化しても位置が一切動かず、ピクトグラムバッジと破線の囲み線のみ追加される（8の再確認）。ボード上のピクトグラムタップでの技変更、コマ長押しでのタンブリング技選択もそれぞれポップオーバー経由で正常動作
- シーンを22件程度まで増やしフッターを横スクロールさせた状態でカードを選択しても、スクロール位置が保持される（3）。「前のシーン」「次のシーン」ボタンでの前後移動も正常（4）
- 実際にYouTube再生を進行させ、`startSec`を跨ぐたびにフッターの該当カードへハイライトが正しく移動することを確認（9）
- プレビュー開始で音源が自動再生され、プレビュー終了で一時停止することを確認（10）
- エクスポートメニューに「コンタクトシート（選択中の1シーン）」ボタンが存在（11）。「全シーン一覧をPDF」が2列×4行の`.print-page`グリッド（`@page{size:a4}`）を生成することをDOM構造で確認（12）
- ホーム画面のサブタイトルが削除されている（13）
- ホーム画面「プロジェクトを開く」：`localStorage`に一覧を仕込んだ状態で開くと、現在編集中のプロジェクト＋一覧内の各プロジェクトが表示され、「×」での一覧からの削除が即座に反映されることを確認（14）

## 完了：2026-09-02 追加フィードバック5項目＋頑健化

前回の追加フィードバック対応後、オーナーから5点の追加修正依頼があり対応した。すべて実装・実機（Browserツールでのエミュレーション）確認済み。

1. **スタンツの技による広がり方の違い**：`STUNT_TYPES`の`spacing`値がフルアップ/エクステンションのみ6、スタックのみ4など不揃いだったため、技を切り替えるとグループサイズが変わって見えた。全種類`spacing:5`に統一。実機確認：フルアップとスタックでコマ間距離が両方とも正確に50.0で一致
2. **名簿欄の幅がサイドパネルに収まらない**：`.roster-row`の名前入力・パート選択に明示的な`flex`/`width`指定がなかったため、選択肢の長いテキスト（「バックスポット」等）分だけ最小幅が広がり、パネル幅を超えていた。色スウォッチ26px・パート選択64px固定・名前入力は残り幅いっぱい(`flex:1;min-width:0`)に調整。実機確認：行幅202px＜パネル幅246px、横スクロールなし
3. **コマの長押しでテキスト選択される**：`.koma`と`.stage-svg`に`user-select:none`/`-webkit-user-select:none`/`-webkit-touch-callout:none`を追加
4. **メニュー非表示時にボード横へカウント別メモを表示**：`renderStagePanel()`を`.stage-row`（横並びのflexコンテナ）でラップし、`!ui.sidebarOpen`のときだけ`renderCountNotesPanel()`（8カウント分の`.count-note-cell`を縦に並べたもの、シーンタブの`sc.countNotes`と同じデータ・同じ`data-act="count-note"`を再利用）をボードの右に表示
5. **ホーム画面文言・構成名の入力必須化**：「プロジェクトを編集」→「プロジェクトを開く」に変更。ウィザードに「構成名」必須テキスト欄を追加し、未入力時は`alert`で作成をブロック。入力した値が`state.title`（プロジェクト名としてヘッダーに表示）になる

**副産物のバグ調査（結果的に実装のバグではなかった）**：4番の`.stage-row`導入直後、Browserツールでの検証中に「ボードが160×112に極小化する」現象が再現し、一度は`.editor-body`に`grid-template-rows:minmax(0,1fr)`、`.stage-row`に`flex:1 1 auto`を追加する修正を入れた（これ自体は無害かつCSSの堅牢性向上として有効なので残している）。詳しく追跡した結果、真因はテストツール側の`resize_window`（ビューポートエミュレーション）が実際の`window`の`resize`イベントを発火しないため、`fitStageFrame()`が新しいサイズで再計算されないまま古い（小さい）インライン`width`/`height`が残り続けるという**検証環境特有の現象**で、アプリ自体のロジックは正しかった。とはいえ実運用でも「何らかの理由でresizeイベントが発火しない」ケースへの保険として、`ResizeObserver`で`.editor-body`のサイズ変化を直接監視し`fitStageFrame()`を呼ぶように変更した（`window.resize`イベントへの依存を減らし、より頑健に）。

## 完了：2026-09-01 オーナー指示の8項目

前回引き継ぎ時点でリストしていた8項目はすべて実装・実機（Browserツールでのエミュレーション）確認済み。

1. **iPad対応**：`#root`の`height`を`100vh`→`100dvh`（フォールバック併記）に変更。iOS Safariの動的ツールバーによる`100vh`ズレが「常にスクロールバーが出る」の主因だった。加えてグリッドのブレークポイントを920px→1024px（音源パネル幅を`min(280px,34vw)`に縮小）/680px（完全スタック化）の2段階に整理。iPad Mini縦(768×1024)〜iPad Pro横相当(1180×820)まで、ページ全体のスクロールが出ないことを確認済み
2. **複数選択時ツールバー位置**：`renderStagePanel()`でDOM順序を`stage-frame`→`sel-toolbar`に入れ替え。ボード自体の表示位置は選択状態に関わらず固定される
3. **配置タブの折りたたみ化**：「出演メンバー（このシーン）」「配置のプリセット」を`<details class="collapse">`化（デフォルト閉）
4. **コマのサイズ**：`komaScale`の上限を140→220に拡張
5. **スタンツ隊列の間隔・選択UI**：
   - **根本原因が判明**：SVGの座標系はX方向1%=10VB単位（`VB_W=1000`）だがY方向1%=7VB単位（`VB_H=700`）で物理スケールが異なる。`topOffset`/`spotterOffset`を横方向の`baseSpacing`と同じ感覚の数値（6〜8）にしていたため、縦方向では実際にはコマ直径48VB単位を下回り重なっていた。`topOffset`/`spotterOffset`を13〜20に引き上げ、ハンズ/ベース行の固定オフセットも±7/-2→±8に修正して解消。さらに`layoutStuntGroup()`で`komaScale`に比例して間隔もスケールするよう変更（コマを大きくしても密度感が変わらない）
   - **ピクトグラム選択UI**：`renderStuntPictoPicker()`を新設し、`<select>`をピクトグラムボタンの横スクロールリストに置き換え。クリックで即座に技を適用/変更（別途「適用」ボタン不要）。新規作成（`create-stunt-pick`）と既存グループの技変更（`stunt-list-pick`）の両方に使用
6. **ガイド線の不具合・仕様変更**：三分割線は表示ロジック自体は正常だったが後述7の配色問題で見えにくかった。中央線は前後4分割線（`VB_H`を4等分する横線3本）を追加
7. **ガイド線の視認性**：中央線=teal、対角線=amber、三分割線=violetとそれぞれ別配色にし、不透明度も0.6〜0.7→0.7〜0.9に引き上げ
8. **8カウント自動分割のBPM方式化**：`ui.countSplit`を`{markA, markB}`（2点マーク）から`{bpm, markA}`（BPM入力＋1点マーク）に変更。間隔は`60/bpm*8`で自動計算。`renderTimeline()`のプレビュー用グリッド、`generateCountScenes()`の生成ロジックも合わせて変更

## 完了：2026-09-01 上記8項目への追加フィードバック対応

オーナーが上記8項目を実機（iPad）で確認した結果のフィードバック。1・3・5・6は追加修正、それ以外は新規要望。すべて実装・Browserツールでのエミュレーション確認済み（1のタッチバウンスのみ実機のみで確認可能、コード上は標準的な対策を適用）。

1. **iPad：画面がドラッグで動く**：`html,body`に`overflow:hidden; overscroll-behavior:none;`を追加。iOS Safari特有のバウンス（ラバーバンド）スクロールを抑止する標準的な対策
2. **中央テーブルと右サイドテーブルの比率が崩れていた（根本原因判明）**：`.side-panel`（グリッドの直接の子要素）に`overflow`が未設定だったため、CSS Gridの「暗黙のminmax(auto,1fr)」規則により、内部コンテンツ（スタンツ一覧のピクトグラムピッカー等、幅の広い要素）のmin-content幅がそのまま`.side-panel`の最小幅として要求され、`2fr`側（ステージ）を28pxまで潰していた（実測：修正前28px:836px、修正後492px:246px=正確に2:1）。`.side-panel{ overflow:hidden; min-width:0; }`を追加して解消
3. **折りたたみの文字サイズ**：`details.collapse > summary`のCSSを`h2`と同じOswald/12px/uppercaseに統一
5. **スタンツの重なり（人数に応じた図形配置）**：`autoComposition`のロール順（トップ→ハンズ→ベース→スポッター）をそのまま「図形の上から詰める順番」として使う設計に変更。`stuntShapeOffsets(n, unit)`で人数nに応じた頂点座標（3人=正三角形、4人=ひし形、5人=台形、6人以上=同心円リング）をVB単位で計算し、X/Yそれぞれ÷10・÷7でパーセントに変換（SVGのviewBoxが10:7でCSS側もaspect-ratio:10/7なので、VB単位で計算すれば描画結果は正しく正多角形になる）。実機確認：3人グループの3辺すべて50.0（正三角形）、4人グループが上下左右対称（ひし形）、5人グループが上2人・下3人（台形）であることを確認
6. **中央線を縦横4分割に修正**：前回「前後4分割」（横線3本）のみだったのをオーナー指摘で「前後左右4分割」に修正（縦線3本を追加、計6本）
- **ボード上でのスタンツ技変更**：スタンツバッジ（ピクトグラム）を`pointer-events:none`から`.stunt-badge`クリック可能要素に変更。タップすると`renderBoardPopover()`がボード脇にピクトグラム選択ポップオーバーを表示し、選択すると即座に`changeStuntType()`が呼ばれる
- **コマの長押しでタンブリング設定**：`wireStageEvents()`のpointerdownに550msの長押しタイマーを追加（グループに属さない単独コマのみ・移動やpointerup発生時はタイマーをキャンセル）。長押しで「タンブリング」ボタンが出現→タップで技リスト（`TUMBLE_SKILL_ORDER`）を表示→選択で`positions[id].tumbleSkill`を設定
- **シーン全体のメモ欄をボード下に追加**：`renderStagePanel()`に`#sceneMemoInline`テキストエリアを追加。既存の「シーン」タブの`#sceneNote`と同じ`sc.note`フィールドを共有（相互に反映される）
- **ボードが常に中央パネルに収まるサイズに**：CSSのみでの解決が難しかったため、`fitStageFrame()`関数を新設。`.stage-panel`の実際の余白（他の兄弟要素の高さ・パディング）を`getBoundingClientRect()`で測定し、10:7比を保ったまま収まる最大サイズを計算して`.stage-frame`にインラインで`width`/`height`を設定。`render()`の最後と`window.resize`イベントで呼び出す
- **シークバーの最大縮小範囲を拡大**：`tlZoomOut`の下限を8→0.3に、目盛り間隔の閾値も低ズーム域を追加（zoom<3で60秒間隔）。長い曲でも全体を1画面に収められる

**ボードポップオーバーの実装メモ**：`ui.boardPopover`（`{type, gid/memberId, x, y}`）で状態管理。SVG内の`.stunt-badge`クリックとボード外クリックは`wireStageEvents()`のpointerdownで、それ以外（サイドバー等）へのクリックはグローバルな`document.addEventListener('pointerdown', ...)`（アプリ初期化時に1回だけ登録）でポップオーバーを閉じる。

## 現在の状態（2026-08-31 引き継ぎ時点で確認した内容）

### 実装済み機能（コードを読んで確認）

- **ホーム画面 / 新規プロジェクトウィザード**：タイトル、音源（ファイルアップロード or YouTube URL）を設定して開始
- **メンバー管理**：名前・パート（フライヤー/ベース/バックスポット/ダンサー/タンブラー/その他）を登録
- **ステージ設定**：フロアの幅・奥行き（メートル）、グリッド表示、前列/後列の番号ラベル、中央線・対角線・三分割線の補助線、背景画像＋不透明度
- **シーン（隊形）エディタ**：SVGステージ上でメンバーの駒（koma）をドラッグ配置。複数選択・整列プリセットあり
- **スタントグループ**：役割（トップ/ベース/スポッター/ハンズ）と技の種類（通常/エクステンション/バスケットトス/ハーフアップ）ごとに自動配置間隔を計算
- **セクション分類**：オープニング/チャント/ジャンプ/スタント/ピラミッド/バスケットトス/タンブリング/ダンス/つなぎ/エンディングでシーンにタグ付け
- **音楽同期**：音声ファイル or YouTube埋め込みで再生し、シーンと紐付け。カウント（8カウント等）ごとのメモ欄あり
- **プレゼンモード**：フルスクリーンで隊形を1つずつ再生・確認
- **印刷（エクスポート）**：シーンごとに印刷用ページを生成
- **保存**：Claude Artifact環境では`artifact`capability経由（`window.claude.use('artifact')`）。GitHub Pages版ではFirebase Firestoreへのクラウド保存＋共有リンク発行（詳細は下記「データ永続化（Firebase）」参照）。どちらも使えない場合はlocalStorageのみにフォールバック
- **Undo/Redo**：最大40件の履歴
- **8カウント自動分割（2026-08-31追加）**：音源パネルのズーム可能タイムライン上で①「1カウント目」②「次の1カウント目（8カウント後）」の2点を再生位置マークで指定すると、その間隔を8カウント分の秒数として曲の最後まで均等にシーンを自動生成する。BPM解析は行わず、再生位置マークのみで完結するためYouTube音源でも同じ仕組みが使える（詳細は下記）
- **音源パネルの幅調整（2026-08-31追加）**：エディタ画面の音源パネル（左カラム）とステージパネルの境目をドラッグして幅を変更できる。8カウント分割のタイムラインを広く使うための機能。幅はこの端末にlocalStorage保存される（`cheerflow_audio_panel_w`）。画面幅920px未満では1カラム表示になりリサイズ不可

### 未確認・要オーナー確認事項

- 実際にオーナーが今どこまで運用しているか（サンプルデータか、実際のチームの構成を入れて使っているか）は未確認
- 元のArtifact（上記URL）側に実際の作業データ（`app-state`）が入っている可能性がある。今回引き継いだ`cheerflow.html`は空の状態（`app-state`が`{}`）のテンプレートのみで、実データは含まれていない
- **出演メンバー行の移設について（2026-09-01）**：オーナーから「フォーメーションボード下の色と名前の表示は不要」との指摘があった際、UI（`renderPresenceRow()`のピル型ボタン列）は「シーンごとの出演可否を切り替える唯一の手段」だったため機能自体は削除せず、配置タブの折りたたみ式「出演メンバー（このシーン）」セクションへ表示場所だけ変更する対応にとどめた。この対応（表示場所の変更のみ・機能は維持）で意図通りか、それとも機能自体が本当に不要だったのか（別の方法でシーンごとの出演メンバーを管理する予定がある等）を次回オーナーに確認すること

## 次の優先タスク（2026-08-31時点）

### 解決済み：YouTube同期再生のバグ（2026-08-31）

**原因**：コードのバグではなく、`file://`プロトコルでローカルHTMLを直接開いていたことが原因だった。YouTube IFrame Player APIはiframeとの通信に`postMessage`を使うが、`file://`で開いたページのoriginは`null`扱いになり、YouTube側との組み込み許可判定が正しく機能しない（ブラウザによっては無音で失敗し、映像が表示されないだけでエラーも出にくい）。

**確認方法**：GitHub Pages公開後の https://sustrainortho-bit.github.io/cheerflow/ （https配信）で実際にブラウザ操作をエミュレートし、YouTube URLを設定→動画が正常に表示され、再生ボタンでの再生/一時停止の切り替えも正常に動作することを確認した。

**結論**：https（または`http://localhost`のような通常オリジン）で配信されている限りYouTube同期再生は問題なく動作する。オーナーが今後ローカルで動作確認する際は、`file://`で直接開くのではなく、ローカルサーバー経由（例: `npx serve` や VSCodeのLive Server）で開くよう案内すること。

### 完了：公開デプロイ（2026-08-31）

- `cheerflow`リポジトリをpublicに変更し、GitHub Pagesを有効化した
- 公開URL: https://sustrainortho-bit.github.io/cheerflow/ （`index.html`が`cheerflow.html`へリダイレクト。直接開く場合は https://sustrainortho-bit.github.io/cheerflow/cheerflow.html ）
- 注意：Pages上のバージョンは`git push`した時点のコードがそのまま公開される

### 完了：データ永続化 / 共有リンク機能（2026-08-31）

**方式**：Firebase Firestoreへのクラウド保存。他アプリ（[tasksync-hub](https://github.com/sustrainortho-bit/tasksync)がFirebase専用プロジェクトを持つのと同じ慣習で、CheerFlow専用のFirebaseプロジェクト`cheerflow-app`を新規作成した。

| 項目 | 内容 |
|---|---|
| Firebaseプロジェクト | `cheerflow-app`（https://console.firebase.google.com/project/cheerflow-app/overview） |
| DB | Firestore Native、リージョン`asia-northeast1` |
| コレクション | `projects/{自動採番ID}` — 1ドキュメント＝1プロジェクトの`state`全体（JSON） |
| ルール定義 | [firestore.rules](firestore.rules)（`firebase deploy --only firestore:rules --project cheerflow-app`でデプロイ） |
| 設定ファイル | [firebase.json](firebase.json) |

**セキュリティモデル（重要・要認識共有）**：認証なし。「URLにあるプロジェクトID（Firestoreの自動採番ID、事実上推測不可能な文字列）を知っている人は誰でも読み書きできる」という、Googleドキュメントの「リンクを知っている全員が編集可」と同じモデル。ユーザー登録・ログイン機能はまだない。チームの外にリンクを流出させないよう運用でカバーする必要がある。

**動作の流れ**：
1. 新規プロジェクトはまずlocalStorage（この端末のみ）に保存される（今まで通り）
2. 「保存して共有」ボタンを押すと、初回はFirestoreに新規ドキュメントを作成し、URLに`?p=<ドキュメントID>`を付与（`history.replaceState`、ページ遷移なし）。2回目以降は同じドキュメントを上書き
3. `?p=<id>`付きのURLを開くと、ページ読み込み時にFirestoreから該当プロジェクトを取得し、その内容で画面を初期化する（ローカルのlocalStorageの内容より優先される）
4. エクスポートメニューの「共有リンクをコピー」で、現在のURL（`?p=`付き）をコピーできる

**既知の制約**：
- 音声ファイルをアップロードする形式（YouTube URLではなくファイル直接アップロード）の場合、base64化した音声データが`state`に丸ごと入るため、Firestoreの1ドキュメント1MiB上限を超えるとクラウド保存できない（保存ボタンで「音声ファイルが大きすぎて共有できません」と表示され、この端末へのlocalStorage保存のみ行われる）。YouTube URL方式なら問題にならない
- リアルタイム共同編集（Googleドキュメントのような同時編集の自動反映）ではない。「保存して共有」ボタンを押した時点のスナップショットが同期される方式

### 完了：8カウント自動分割機能（2026-08-31）

**経緯**：最初はBPM自動解析（Web Audio APIで波形解析）を検討したが、YouTube埋め込みは生の音声波形にページ側からアクセスできないため原理的に不可能と判明。オーナーの提案で「①1カウント目・②次の1カウント目（8カウント後）の2点を再生位置マークで指定し、その間隔で均等分割」という方式に変更した。これは既存の`playbackTime()`/`playbackSeek()`抽象化（YouTube・ファイル音源共通）だけで完結するため、**YouTube音源でも使える**のが利点。

**実装箇所**：
- `renderTimeline()`（`cheerflow.html`内）：ズーム可能なタイムライン（`.tl-scroll`/`.tl-track`、CSSの`repeating-linear-gradient`で目盛りを描画、実際のDOM要素は秒ラベルのみ）。クリック/ドラッグで`playbackSeek()`を呼ぶ。再生中の再描画は既存の`AUDIO_EL`timeupdate/`startYtPoll`と同じく`render()`を経由せず直接DOM更新（`updateTimelineUI()`）
- `renderCountSplitBox()`：①②マークボタンと生成ボタン
- `generateCountScenes()`：`addScene()`と同じフィールド構成でシーンを一括生成（直前のシーンの隊形・グループをコピーして開始点にする）
- 音源パネル（左カラム）のリサイズ機能（`#audioPanelResizer`、CSS変数`--audio-panel-w`、`localStorage`永続化）もこの機能のために追加

**既知の制約**：
- YouTube音源は`seekTo()`の精度がファイル再生よりやや粗く（キーフレーム単位、数百ms程度のずれが出ることがある）、正確なビート合わせが難しい場合がある。ファイルアップロード音源の方が精密
- 波形の可視化はしていない（YouTubeは技術的に不可能、ファイル音源も今回は未実装）。目盛りと再生音だけを頼りに①②をマークする
- テンポが揺れる曲（生演奏、途中でBPMが変わる曲等）は均等分割が途中でずれる。手動でのシーン追加・削除・時間調整で補正する前提

### 完了：細かいUI修正（2026-08-31）

- シークバー（タイムライン）をドラッグ中、位置に秒数のツールチップを表示（`#tlTooltip`）
- 8カウント自動生成のシーン名を「カウントN」から「シーンN」（既存シーン数から連番）に変更
- YouTube音源で「音だけ表示」ボタンを追加（`ui.ytAudioOnly`）。動画は画面外（`top:-9999px`など）に逃がして再生は止めない方式。`display:none`にすると停止する場合があるため避けた
- フッターのシーンカードを選択すると、そのシーンの`startSec`へ自動シーク（YouTubeは`seekTo`が非同期なので`updateTimelineUI`で即座に見た目を合わせている）

### 完了：ダークテーマ・スタンツ機能拡張・タンブリング表示（2026-08-31）

- **ダークテーマ化**：`:root`の基本トークンを暗色パレットに変更（元々`prefers-color-scheme`/`[data-theme="dark"]`用に用意されていた値を昇格させ、ライト版の分岐は削除）
- **タイムラインのシーン区切り線**：`renderTimeline()`内で`state.scenes`の`startSec`ごとに縦線（`.tl-scene-mark`）を描画
- **±10秒ボタン**：`renderTransportRow()`に「⏪10」「10⏩」を追加、`playbackSeek(playbackTime()±10)`
- **コマの個別カラー**：`member.color`フィールドを追加。人数設定・メンバー追加時に`KOMA_COLOR_PALETTE`（12色）から自動割り当て（`nextKomaColor()`）。名簿タブに`<input type="color">`を追加して個別に変更可能。`renderKomaSvg`は`member.color`優先、未設定時はパート色にフォールバック
- **スタンツ13種＋ピクトグラムスタンプ**：`STUNT_TYPES`をスタンド/ハーフアップ/フルアップ/エクステンション/リバティ/アラベスク/ヒール/スコーピオン/ショルダースタンド/エレベーター/ピラミッド/サイドサイ/スタックの13種に置き換え、スペーシング値を全体的に縮小（コマ同士を密着気味に）。`STUNT_PICTOS`に各技の簡易ピクトグラム（stroke描画のシンプルな棒人間アイコン）を用意し、`renderStuntBadges()`でスタンツグループの重心付近にラベル付きバッジとして表示
  - **設計上の制約（オーナー確認済み・2026-08-31）**：ピラミッド/エレベーター/サイドサイ/スタックは本来複数フライヤーが関わる構成だが、今回は`autoComposition`/`layoutStuntGroup`（1トップ+ベース+スポッター+ハンズの単一構成）を変えず、ラベルとピクトグラムだけ切り替える簡易対応にとどめた。オーナーに確認したところ「1スタンツ1フライヤーで今のところ大丈夫」とのことで、複数フライヤー対応は現時点では不要と判断。別セッションで動いていた検討タスクは目的を終えている

### 完了：スタンツ表示・コマ色の改善（2026-08-31）

前回のスタンツ機能追加後、オーナーから4点のフィードバックがあり対応した。

- **コマの色が全員グレーになる不具合**：`member.color`は新規作成時のみ自動割当していたため、既存プロジェクト（引き継ぎ時点のデータや過去のテストデータ）のメンバーには`color`フィールドがなく、パート色フォールバック（未設定＝「その他」＝グレー）に落ちていた。`ensureMemberColors(state)`を追加し、state読み込み時（ローカル/クラウド/JSON読み込みの全経路）に`color`未設定のメンバーへ自動でパレット色を割り当てるマイグレーション処理を実装
- **スタンツグループの囲み線**：`renderGroupOutlines()`を追加。グループメンバーの位置のバウンディングボックス＋余白に、accent色の角丸破線矩形（`stroke-dasharray:7 5`）を描画してグループの範囲を視覚化
- **ピクトグラムの配置変更**：スタンツの上に重ねて表示していたバッジを、グループの右側（画面端に近い場合は左側に自動切り替え）に配置するよう`renderStuntBadges()`を修正。バッジサイズ・フォントサイズも拡大（7px→11px）して視認性を改善
- **技名の即時表示・変更機能（初版、後に「スタンツ一覧」UIに置き換え）**：選択中の1コマがスタンツグループに属する場合に技を変更できる`changeStuntType()`を追加。ロール構成（トップ/ベース/スポッター/ハンズ）を保持したまま技だけ変更できる
- **さらなる密集化**：`STUNT_TYPES`の間隔値を全体的にさらに縮小（コマ半径24に対しbaseSpacingを3〜4に設定）→ 後述の通りオーナーから「重なっている」とフィードバックがあり調整し直した
- **タンブリング移動の表示**：シーン内の各メンバーの位置データに`tumbleSkill`を持たせ（`sc.positions[id].tumbleSkill`）、配置タブで選択中コマが1つのときに「タンブリング移動」ドロップダウン（ロンダート/ロンダートバク転/バク転/側転/前方展開/後方展開/前転）を表示。設定すると`renderPaths()`で移動線が一点鎖線（`stroke-dasharray:7 3 1 3`、色は`--hue-rust`）になり、技名がコマの近くにテキスト表示される
- **バグ修正（今回の作業中に発見）**：新規プロジェクト作成時、`autoArrangeAll(scene1)`が`state`の更新前に呼ばれていたため、参照する`state.members`が空になり、全コマが中央(50,50)に重なって配置される不具合があった（`createProject`内の実行順序を入れ替えて修正）。おそらく元のArtifact版から存在していた不具合

### 完了：スタンツUIの再修正（2026-08-31）

前回のスタンツ機能に対し、オーナーから追加で6点のフィードバックがあり対応した。

- **間隔の再調整**：「密集させて」→「重なっている」という往復があり、コマ直径48（半径24×2）に対し`baseSpacing:5`（ちょうど触れ合う程度）を基準値として再設定。前回の`baseSpacing:3〜4`は実際には重なってしまっていた
- **「スタンツ一覧」UIを新設（配置タブ）**：スタンツグループを作成すると「スタンツ1」「スタンツ2」...と自動採番されたカードが並び、各カードに専用の技セレクト・「構成を編集」・「解除」ボタンを持つ。以前の「1コマだけ選択したときだけ技変更UIが出る」という仕組みは、グループのコマをクリックすると自動的にグループ全員が選択される仕様と衝突し、技を変更したくても該当UIにたどり着けない／どのグループを編集しているか分かりにくいという実質的なバグになっていたため、グループID直結の永続リストに置き換えた。これが「技名を選択してもボード上で変わらない」の主因と考えられる
  - 実装：`renderPlacementTab()`内で`stuntGroupsOf(sc)`を毎回リスト表示し、各行の`<select data-act="stunt-list-type" data-gid="...">`が`changeStuntType(g, sc, value)`を直接呼ぶ（選択状態に依存しない）
- **役割バッジの表示切替**：`state.stage.showRoleBadges`（デフォルトtrue）を追加。ステージタブにオンオフスイッチを設置し、`renderKomaSvg()`内でトップ/ベース/スポッター/ハンズの小バッジ表示を切り替え可能に
- **フォーメーションボード下の「出演メンバー」行を配置タブへ移動**：常時表示だったピル型のメンバー一覧（色ドット＋名前、シーンへの出演可否を切り替える）をステージ下から削除し、機能自体は維持したまま配置タブの上部「出演メンバー（このシーン）」セクションに移設（`renderPresenceRow()`）
- **右サイドパネルの幅比率を修正**：音源パネルのリサイズ機能を実装した際に`.editor-body.with-sidebar`のgrid-template-columnsが誤って`1fr 1fr`（中央:右=1:1）になっていた（本来は`2fr 1fr`＝2:1）。デグレを修正

## 開発の進め方

- このファイルは単一HTMLなので、ビルドコマンドは不要。ブラウザで直接開けば動作確認できる
- 変更したら区切りの良いタイミングで`git commit`→`push`する
- Claude Artifactとして公開・更新する場合は、Artifactツールで`cheerflow.html`をpublishする（オーナーが元のURLを教えてくれた場合はそのURLを指定して上書き更新できる）
